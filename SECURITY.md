# 安全政策（Security Policy）

> English version: [SECURITY.en.md](SECURITY.en.md)

## 受支持的版本

我们当前支持 Keystone 1.x 的最新稳定次版本线。安全修复会向后移植到最近的发布版本。

| 版本    | 是否支持               |
| ------- | ---------------------- |
| 1.x     | :white_check_mark:     |
| < 1.0   | :x:                    |

## 报告漏洞

如果你在 Keystone 中发现了安全漏洞，请**不要**提交公开的 GitHub issue。

请通过以下任一私密渠道报告：

- **GitHub 私有漏洞报告（Private Vulnerability Reporting）**——在仓库的
  **Security** 标签页点击「Report a vulnerability」按钮。这是首选渠道。
- **电子邮件**——将详情发送至 **security@keystone.dev**（请替换为项目所有者的
  地址）。如可行请使用加密。

请尽量包含：

- 漏洞描述及其影响
- 复现步骤（如有可能附上概念验证）
- 受影响的版本与环境
- 已知的可行缓解措施（若有）

### 处理流程

1. **确认收到**：5 个工作日内回复。
2. **研判与确认**：我们将复现并评估严重等级。
3. **协同披露**：目标在确认后 90 天内发布修复并公开安全公告；若存在可行
   的临时规避方案，会提前发布。
4. **致谢**：在获得你许可的前提下，我们会在安全公告中致谢报告者。

## 安全设计说明

Keystone 默认将插件视为**同进程可信代码**（见
`docs/architecture/02-plugin-model.md` 与 ADR-0001）。信任边界是安装该插件的
用户。如果你的部署需要更强的隔离（例如加载不可信的第三方插件），在进程隔离
扩展点（`IPluginHost`，ADR-0001 决策 1）可用之前，请勿加载不可信来源，并考虑
将插件运行在独立的、沙箱化的宿主进程中。
